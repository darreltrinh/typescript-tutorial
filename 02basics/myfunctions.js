"use strict";
exports.__esModule = true;
// add a 2 to an input number
function addTwo(num) {
    return num + 2;
}
function getUpper(val) {
    return val.toUpperCase();
}
function signUpUser(name, email, isRich) {
    // do something
}

// var is default behaviour can update to const in tsconfig file
var loginUser = function (username, gotMoney, age) {
    if (gotMoney === void 0) { gotMoney = true; }
};
addTwo(5);
getUpper("wavey");
signUpUser("wavey", "wavey.seal@wavey.com", true);
loginUser("wavey", true, 27);
